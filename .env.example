# =============================================================================
# Environment Variables — Frappe Books (WillNeill/books fork)
# =============================================================================
# Copy this file to .env.publish and fill in real values for CI/CD publishing.
# For local development, none of these are required — the app runs fine
# without them (Sentry/PostHog silently disable, builds skip code signing).

# -----------------------------------------------------------------------------
# GitHub — Release publishing
# -----------------------------------------------------------------------------
# Personal access token with repo scope, used by electron-builder to publish
# draft releases to GitHub Releases.
GH_TOKEN=

# -----------------------------------------------------------------------------
# Apple — macOS code signing & notarization
# -----------------------------------------------------------------------------
# Required only for signed macOS builds (CI/CD publishing).
# Local dev builds use --nosign to skip code signing entirely.
APPLE_ID=
APPLE_APP_SPECIFIC_PASSWORD=
APPLE_TEAM_ID=
# Signing identity — must be "Developer ID Application: ..." for distribution.
# Find yours with: security find-identity -v -p codesigning
# Without this, electron-builder falls back to ad-hoc signing (other Macs reject it).
# IMPORTANT: Quote the value — it contains spaces and parentheses.
# IMPORTANT: Use the team/org name and ID only — do NOT include the
# "Developer ID Application:" prefix; electron-builder adds it automatically.
CSC_NAME="Your Name or Org (TEAMID)"

# -----------------------------------------------------------------------------
# Sentry — Error reporting (@sentry/electron)
# -----------------------------------------------------------------------------
# DSN from your Sentry project settings (Settings > Projects > Client Keys).
# Injected at build time via esbuild/Vite `define` blocks.
# When empty, Sentry is silently disabled — no errors are sent.
SENTRY_DSN=

# -----------------------------------------------------------------------------
# PostHog — Usage analytics (posthog-js)
# -----------------------------------------------------------------------------
# Project API key and host from PostHog project settings.
# Injected at build time into the renderer process only.
# When empty, PostHog is silently disabled — no events are sent.
POSTHOG_KEY=
POSTHOG_HOST=

# -----------------------------------------------------------------------------
# Development — Optional overrides
# -----------------------------------------------------------------------------
# Override the Vite dev server bind address and port (defaults: 0.0.0.0:6969).
# VITE_HOST=0.0.0.0
# VITE_PORT=6969
