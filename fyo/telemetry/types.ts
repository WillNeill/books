export enum Verb {
  Started = 'started',
  Completed = 'completed',
  Created = 'created',
  Deleted = 'deleted',
  Submitted = 'submitted',
  Cancelled = 'cancelled',
  Imported = 'imported',
  Exported = 'exported',
  Printed = 'printed',
  Closed = 'closed',
  Opened = 'opened',
  Resumed = 'resumed',
}

export enum ErrorLogEnum {
  IntegrationErrorLog = 'IntegrationErrorLog',
}

export type Noun = string;
